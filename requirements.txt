# YES Bank AI PoC -- Python Dependencies
# Install with: pip install -r requirements.txt

# -- Core Data Science ------------------------------------------------
pandas>=1.5.0
numpy>=1.23.0

# -- Machine Learning -------------------------------------------------
scikit-learn>=1.1.0

# -- Excel Reading ----------------------------------------------------
openpyxl>=3.0.0

# -- Flask Web Server (app.py) ----------------------------------------
flask>=2.0.0

# -- LLM Providers (app.py) -------------------------------------------
groq>=0.4.0                  # GROQ cloud LLM (free tier available)
requests>=2.28.0             # Used for Ollama HTTP calls

# -- FastAPI Transcription Server (huddle_generater.py) ---------------
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6      # Required for FastAPI Form/File uploads
httpx>=0.24.0                # HTTP client for Sarvam.ai API calls

# -- Transcription Engines (huddle_generater.py) ----------------------
# Install at least one:
openai-whisper>=20231117     # Option A: OpenAI Whisper (local, free)
# faster-whisper>=0.10.0     # Option B: Faster Whisper (4x faster, CPU-optimised)

# -- LLM Enrichment (huddle_generater.py, optional) -------------------
# anthropic>=0.20.0          # Uncomment for Claude Sonnet LLM enrichment

# -- Visualisation (optional) -----------------------------------------
# plotly>=5.0.0              # Uncomment to export chart images

# -- PDF Parsing (optional) -------------------------------------------
# pdfplumber>=0.9.0          # Uncomment when integrating real RBI PDFs

# -- NOTES ------------------------------------------------------------
# ffmpeg must be installed separately (not a Python package):
#   Windows: winget install ffmpeg  OR  choco install ffmpeg
#   Mac:     brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
#
# For fully offline LLM (no API key needed):
#   pip install ollama  -- then run: ollama pull llama3
